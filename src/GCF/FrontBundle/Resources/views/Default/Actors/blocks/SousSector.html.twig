{%  if key == 0 %}
    {% set classActive = 'in' %}
{% else %}
    {% set classActive = ' ' %}
{% endif %}

<div id="collapse{{ key }}" class="panel-collapse collapse {{ classActive }}">

    <ul class="list-group">
        {% for key, sectorFil in sectorsFils %}
            {% if sectorFil.secteurActeurParent == sector.nom %}
                <li class="list-group-item" onclick="getFils('{{ sectorFil.nom }}')" style="cursor: pointer;"><a style="text-decoration: none;">{{ sectorFil.nom }}</a></li>
            {% endif %}
        {% endfor %}
    </ul>

</div>